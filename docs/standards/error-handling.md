# Error Handling

Status: NOT_INITIALIZED

Document verified conventions for error creation, wrapping, classification,
transport mapping, retryability, logging ownership, and sensitive-data
redaction.

Identify boundaries where errors cross HTTP, RPC, message, transaction, or
process interfaces. Cite implementation and test evidence for each material
rule. Record inconsistent and risky patterns separately.
